# Currency-Converter-rs
just a small currency converter i created in rust as an exercise

how to use:

1) install rust

2) rename the file ".env_TEMPLATE" to ".env" and put your api key that you can get from https://app.freecurrencyapi.com in-between the ""

3) $ cargo run --release
